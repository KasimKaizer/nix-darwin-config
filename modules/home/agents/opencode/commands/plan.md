---
description: Explore requirements and create an actionable implementation plan without editing product code.
agent: planner
---

Before planning, call the `skill` tool with `name: "work-plan"` and follow its complete
workflow exactly. The skill controls the planning process: use its required plan-mode
announcement, intent routing, read-only exploration, decision/approval gate, plan
artifact scaffolding, and execution handoff. Do not substitute a short ad-hoc plan for
that workflow.

Plan this request using that skill: $ARGUMENTS
